<% if users.any? %>
  <ul>
    <% users.each do |user| %>
      <li>
        <%= user.name %>
        <p><%= link_to 'View profile', taskuser_path(user) %></p>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>